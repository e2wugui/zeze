
#### TODO

Zeze
	1 使用Raft改造GlobalCacheManager。2 使用自己的复制服务器。
	global只提供备份服的话，不用Raft，可以自己实现一个简单的复制（先复制远程，再修改本地）
	这样就可以在主服失败时切换到备份服，这里关键的问题和确认gs down掉一样，主服的失败怎么
	确认的问题，切换的时候，要所有的gs都切换，否则就有出错的可能。
	对，就是新global在启用前，需要把所有的gs切换到自己上面。这里也有点问题，所有的gs这点
	有点麻烦，当然也是极端情况了。新global可以查看本机已分配锁的所有gs，通知这些都切换过来，
	然后才启用。如果没有分配，相当于新增的gs，逻辑上是对的。这里有个极端情况是，已分配锁
	的gs通知不到了，怎么处理，切换不切换。可靠点就不切换，导致服务停止，但不会出错。

	打包和更新工具
	每事务提交
	async await?
	GlobalTaskOneByOneByKey？？？
	AsyncSocket.Connect Timeout 可配置，不是很有必要，外面需要同步等待时，可以设置自己的等待时间，不需要依赖Socket.Timeout。

	lua beankey 问题。

Game2
	client: 可靠消息实现的错误检查。

ConfigEditor
	自动完成: Foreign
	自动完成：id Load 的时候记录 maxid，以后编辑AddRow都使用这个递增。
	更多自动完成？普通的列默认最近使用的n个值，根据输入在列中查找最匹配的。
	增加一个工作线程，所有数据操作都放到这里面，和UI线程交互。看情况再决定做不做。
	enum 现在不支持引用在其他文档定义的，有需要了再来加。

senderIsShare 优化。
InstallSnapshot （断点续传）
	发送检测Leader，忽略网络错误，持续发送。
	结果增加重新定位Offset的能力。
