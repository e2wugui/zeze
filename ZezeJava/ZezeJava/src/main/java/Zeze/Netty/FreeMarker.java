package Zeze.Netty;

public class FreeMarker {
	/*
	private final Configuration freeMarker;

	public FreeMarker() {
		freeMarker = new Con
	}

	public void sendResponse(HttpExchange x, Object modalBean) {
		var tmpl = freeMarker.getTemplate(url_path + Name);
		tmpl.write(out, modalBean); // MemoryOutputStreamWriter();
		x.send(memroy.content(););
		x.sendStream(memroy.context(););
	}
	*/
}
