apply from: '../ZezeJava/build.gradle'

dependencies {
    implementation project(":ZezeJava")
}

sourceSets {
    main {
        java {
            srcDir "src"
            srcDir "Gen"
        }
    }
}
